<%= form_for(@exercise) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% @exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise_type_id %><br>
    <%= f.select :exercise_type_id, options_for_select([["Endurance", 1], ["Strength", 2], ["Flexibility", 3], ["Balance", 4]]) %>
  </div>
  <div class="field">
    <%= f.label :burn %><br>
    <%= f.number_field :burn %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :time_spent %><br>
    <%= f.number_field :time_spent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
